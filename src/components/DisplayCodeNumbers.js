import React from 'react';

function DisplayCodeNumbers({ codeNumbers }) {
    return (
        <div>
            
            {codeNumbers[0]}
            {codeNumbers[1]}
            {codeNumbers[2]}
            {codeNumbers[3]}
            
        </div>
    )
}

export default DisplayCodeNumbers;
